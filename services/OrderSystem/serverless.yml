service: order-system

 environment:
      NODE_ENV: staging 

plugins:
  - serverless-bundle

provider:
  name: aws
  runtime: nodejs12.x
  region: us-east-1
  stage: dev

functions:
  getOrders:
    handler: handler.endpoint
    events:
      - http: GET /

