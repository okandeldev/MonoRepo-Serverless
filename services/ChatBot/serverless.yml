service: chat-bot

plugins:
  - serverless-bundle

provider:
  name: aws
  runtime: nodejs12.x
  region: us-east-1
  stage: dev

functions:
  get:
    handler: handlers/ChatAPI.webhook
    events:
      - http: GET /